return {
  highlight = {
    enable = true,
    additional_vim_regex_highlighting = false,
  },

  indent = {
    enable = true,
  },

  -- 🔴 Matikan language injection (penyebab warna aneh di HTML)
  injections = {
    enable = false,
  },
}
